# setup.py

from setuptools import setup, find_packages

setup(
    name='simple_linter',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
    test_suite='tests',
)
